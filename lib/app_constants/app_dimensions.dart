import 'package:flutter/material.dart';

class AppDimensions {
  static const double zero = 0.0;

  static const double mainSpace = 16.0;
  static const double doubleMainSpace = mainSpace * 2.0;
  static const double tripleMainSpace = mainSpace * 3.0;
  static const double halfSpace = mainSpace / 2.0;
  static const double quarterSpace = mainSpace / 4.0;
}
